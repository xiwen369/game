server:
  port: 9010
spring:
  application:
    name: game
  swagger:
    basePackage: com.qiqiji.game.controller
    title: game
  datasource:
    url: jdbc:mysql://132.232.219.246/user_data?useSSL=false
    username: whk115
    password: whk115WHK
    dbcp:
      validation-query: SELECT 1
      test-while-idle: true
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
logging:
  level:
    root: INFO
    org.hibernate: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.hibernate.type.descriptor.sql.BasicExtractor: TRACE
    com.springms: DEBUG